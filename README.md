# Simple Whitelist Contract

Simple smart contract holding whitelisted user balances

test with `yarn test`

to deploy:
- run `cp .env.tpl .env`
- fill out envs in .env
- run `yarn deploy:sepolia`
- run `yarn verify:sepolia`
